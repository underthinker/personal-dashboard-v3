{
  "name": "ikigai-dashboard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "db:types": "supabase gen types typescript --linked > src/sync/db-types.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "idb": "^8.0.0"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
